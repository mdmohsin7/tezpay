import 'package:flutter/material.dart';

class DappBrowserPage extends StatelessWidget {
  const DappBrowserPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
